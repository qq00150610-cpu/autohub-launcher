<?xml version="1.0" encoding="utf-8"?>
<!-- 插件卡片组件：导航/音乐/主题 -->
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/plugin_card_container"
    android:layout_width="match_parent"
    android:layout_height="@dimen/plugin_card_height"
    android:background="@drawable/bg_plugin_card"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <!-- 插件图标 -->
    <ImageView
        android:id="@+id/plugin_icon"
        android:layout_width="@dimen/plugin_icon_size"
        android:layout_height="@dimen/plugin_icon_size"
        android:scaleType="centerInside"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@id/plugin_text"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        tools:src="@drawable/ic_navigation" />

    <!-- 插件文字 -->
    <TextView
        android:id="@+id/plugin_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/spacing_xs"
        android:textColor="@color/text_primary_dark"
        android:textSize="@dimen/text_size_xs"
        android:maxLines="1"
        android:ellipsize="end"
        app:layout_constraintTop_toBottomOf="@id/plugin_icon"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        tools:text="导航" />

    <!-- 状态指示点（如正在运行） -->
    <View
        android:id="@+id/status_indicator"
        android:layout_width="@dimen/status_indicator_size"
        android:layout_height="@dimen/status_indicator_size"
        android:background="@drawable/bg_status_indicator"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="@dimen/spacing_xs"
        tools:visibility="visible" />

</androidx.constraintlayout.widget.ConstraintLayout>
